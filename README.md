#RAG-Cyber-Detection

This project has been fully dockerized to ensure it runs consistently across all systems and environments.
The commands to run it are:
1)docker compose build --no-cache
2)docker compose up
